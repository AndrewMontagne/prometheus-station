/// Fired when we lose connection to a cable network, for whatever reason.
/obj/machine/proc/on_network_lost()
	//NOOP, consider trying to re-acquire a network connection

/// Fired when the node we are connected to changes network.
/obj/machine/proc/on_network_changed()
	//NOOP
